import React from "react";
  
    const Button = (props) => {
        return <button onClick={props.callApi}>
         Click for a joke! Hahahahaha hej
         </button>;
    }
  
export default Button;